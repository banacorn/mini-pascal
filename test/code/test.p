program foo(boo);

    var a, b, c, d: integer;

    function addition(a, b: integer) : integer;
        var c, d, e, f: integer;
            begin
                return 1
            end;

    function id(a: integer) : integer;
        begin
            return a
        end;

    // compound statements, "main"
    begin
        putchar(id(98) + 5)
    end.
